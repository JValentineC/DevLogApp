# NearlyFreeSpeech MySQL/MariaDB Connection
# Copy this file to .env and fill in your actual credentials
DB_HOST=devlogs.db
DB_USER=jvc
DB_PASSWORD=your_password_here
DB_NAME=devlogs

# Application Settings
NODE_ENV=development
PORT=3001

# JWT Configuration (IMPORTANT: Change this in production!)
# Generate a strong secret: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# API Configuration for Frontend
# Production: Use NFS backend
VITE_API_URL=https://devlogs-api.nfshost.com/api

# Local development: Uncomment this line and comment the one above
# VITE_API_URL=http://localhost:3001/api
