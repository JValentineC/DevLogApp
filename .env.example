# NearlyFreeSpeech MySQL/MariaDB Connection
# Copy this file to .env and fill in your actual credentials
# Note: Special characters in password should be URL-encoded in DATABASE_URL (? = %3F, @ = %40, etc.)
DATABASE_URL="mysql://YOUR_DB_USER:YOUR_PASSWORD_URL_ENCODED@YOUR_DB_HOST:3306/YOUR_DB_NAME"

# Database credentials for mysql2
DB_HOST=your-database-host.db
DB_USER=your_database_user
DB_PASSWORD=your_secure_database_password
DB_NAME=your_database_name

# Application Settings
NODE_ENV=development
PORT=3001

# JWT Configuration (IMPORTANT: Generate a strong secret!)
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Or: openssl rand -base64 64
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET
JWT_EXPIRES_IN=7d

# API Configuration for Frontend
# Production: Use your production backend URL
VITE_API_URL=https://your-api.nfshost.com/api

# Local development: Uncomment this line and comment the one above
# VITE_API_URL=http://localhost:3001/api
